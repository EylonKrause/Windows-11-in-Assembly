# build artifacts
*.obj
*.exe
*.pdb
*.ilk
*.exp
*.lib
*.map
*.dcv
# disassembly dumps that are regenerated (keep only curated excerpts in RESULTS.md)
*.rawdisasm
# editor
.vs/
*.user

# live-substitution build artifacts
live-substitution/*.obj
live-substitution/*.exe

# local notes, not part of the project
Instructions for Claude.txt

# Machine-local re-validation state: the watched-DLL hash baseline, sweep reports and per-change
# logs are all specific to the PC and the Windows build they were taken on, and are regenerated by
# tools/revalidate.ps1 on demand.
revalidation/

# audit build artifacts
audits/*/*.obj
audits/*/*.exe

tools/__pycache__/
