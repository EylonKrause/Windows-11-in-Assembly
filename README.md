# Windows 11 in Assembly

Aggressive, binary-level superoptimization of a live Windows 11 install — one validated change at a time.

The goal, stated as the "Choice B" target: take the routines this operating system actually runs and
replace them with hand-written assembly that is measurably faster, then prove the win on real hardware
before anything lands here. Over time this accretes into an image whose hot paths are hand-optimized ASM.

This is a research project on **Eylon Krause’s own machine**. It is public as of 2026-09-14, and
it does not redistribute any Microsoft binary — only original assembly, references, tests and
measurements.

---

## What this repository actually is

Each entry under [`changes/`](changes/) is one **optimization unit**: a single Windows-used routine,
reimplemented in assembly, with everything needed to prove it:

```
changes/<NNN>-<name>/
  reference.c      # a plain, obviously-correct scalar reference (the correctness oracle)
  impl.asm         # the hand-written assembly implementation
  correctness.c    # bit-exact / behavior test: impl vs reference vs the SYSTEM function on this PC
  bench.c          # benchmark: impl vs the system function, same inputs, pinned core, warm cache
  RESULTS.md       # the measured numbers on this machine + the disassembly that was compared
  build.bat        # assemble + link + run, reproducible
```

A change is **synced to this repo only after** it passes all three gates on this PC:

1. **Correctness** — bit-exact against the reference across a fuzzed input corpus, and behavior-identical
   to the real Windows function it replaces (same return, same output bytes, same edge cases: empty,
   unaligned, huge, boundary lengths).
2. **Speed** — a real, repeatable win over the shipped system implementation on this hardware. No win,
   or a regression on any measured size class, means it does **not** land. (Same rule the author's NCCL /
   BLIS contributions ran under: never ship a non-win, never regress.)
3. **ABI** — the implementation preserves every Win64 non-volatile register. Run by
   [`tools/abi-check`](tools/abi-check/) (dynamic: sentinels in all of them across a real call) and
   [`tools/abi-audit.py`](tools/abi-audit.py) (static: every `.asm` in the repo).

If a routine can't be beaten, that is recorded honestly in its `RESULTS.md` and the change is not merged.

### Why gate 3 exists

Gates 1 and 2 are structurally blind to one whole class of bug, and it cost this project sixteen
changes. Win64 makes `xmm6`–`xmm15` **callee-saved** — only their low 128 bits; the upper halves of
`ymm6`–`ymm15` are volatile — and sixteen implementations here used them as scratch anyway. Every one
passed both gates: a function that clobbers `xmm6` still returns exactly the right bytes at exactly the
right speed. It corrupts only a *caller* that happened to be holding a `double` there, which no
correctness test comparing integers and strings can observe.

It surfaced by luck. Change 202's benchmark keeps its timing accumulators in `xmm6`/`xmm7`, so a
perfectly correct function reported `0.00 ns`. That was the only symptom, in the only change whose
harness happened to sit on the damage. Gate 3 removes the luck: it fills every non-volatile register
with a sentinel, makes a real call, and reports what did not survive.

All sixteen are fixed, and none of the fixes cost speed — the register pressure was relieved
algebraically rather than by spilling. The recurring trick: a range test written
`(c > LO) AND (HI+1 > c)` needs its second constant as the *first* compare operand, so it has to live
in a register; rewritten `(c > LO) AND NOT (c > HI)` both compares take `c` first, the constants become
memory operands, and the two `AND`s collapse into one `vpandn`. Same instruction count, three fewer
live registers. Several of the rewritten functions came out measurably *faster* than before, because
the constant broadcasts left the prologue (042 `_wcsicmp`: 7.66× → 8.36×).

---

## The validation bench (ground truth for every number here)

See [`docs/PLATFORM.md`](docs/PLATFORM.md) for the full capture. Summary:

| | |
|---|---|
| OS | Windows 11, 25H2, build **26200.8655** (registry `ProductName` still reads "Windows 10" — known stale key) |
| CPU | AMD Ryzen 9 5950X — Zen 3, family 19h, 16C/32T |
| ISA available to emit **and run** | x86-64 + SSE4.2 + AVX + **AVX2** + FMA + BMI1/**BMI2** + POPCNT + LZCNT + ADX + **SHA-NI** + **VAES** + **VPCLMULQDQ** |
| ISA **not** present | **AVX-512** (no), GFNI (no) |
| Secure Boot | ON |
| HVCI / VBS | OFF |
| Toolchain | MASM `ml64` 14.50, MSVC `cl`/`link`, `dumpbin` (VS 2026 Build Tools) |

**Consequence for Choice B:** on this bench the aggressive gains come from AVX2 / BMI2 / VAES / SHA-NI,
not AVX-512. Any implementation intended to be portable off this machine must CPUID-dispatch at runtime
and carry a baseline fallback — an AVX-512 or GFNI path may exist in source but cannot be *validated here*.

---

## Honest constraints (read before assuming "replace the whole OS")

The literal reading — swap every binary in `C:\Windows` for an ASM rewrite and boot it — is **not**
achievable, and pretending otherwise wastes effort. The real, hard blockers:

- **Code signing.** System binaries are catalog-signed. A modified `ntoskrnl.exe` / `win32kbase.sys` /
  driver fails Kernel-Mode Code Signing and will not load while Secure Boot is on; loading a patched one
  means disabling Secure Boot + enabling test-signing, i.e. leaving the supported/secure configuration.
- **No source.** These binaries ship as optimized machine code with no source. "Rewriting in ASM" means
  disassembling, hand-optimizing a *specific routine*, and reassembling with an identical ABI, exports and
  behavior. That is tractable per-routine; it is not tractable for a whole multi-megabyte binary.
- **Servicing overwrites.** Windows Resource Protection + TrustedInstaller ownership + Windows Update will
  revert anything changed in place. Any live patch is temporary by design.
- **This machine has failing RAM** (documented: WHEA machine-checks, BCD-blacklisted pages). It is a poor
  place to run microbenchmarks and a dangerous place to patch boot-path binaries. Benchmarks here are
  treated as directional; publishable numbers want a clean box (the author's Zen4 laptop).
- **Not redistributable.** No Microsoft binary, modified or not, is committed here. This repo holds only
  original assembly, references, tests, and measurements.

### So what "validated here" means in practice

Every change compares our ASM against the **actual function on this PC**, loaded live from its system DLL
(`ucrtbase.dll`, `ntdll.dll`, `kernel32.dll`, …) via `GetProcAddress`, on identical inputs. That is the
strongest honest form of "we replaced what this PC runs and it's faster." **Injecting** the win into the
live system (hot-patch, DLL redirection, or a rebuilt component) is a separate, explicitly-gated step that
requires admin + test-signing and is never done automatically, never on the boot path, never without a
full backup. Correctness-and-speed-proven routines accumulate first; deployment is downstream of proof.

---

## Method

Adapted from the author's upstream-contribution playbook (NCCL, BLIS, nixl, microsoft/STL):

1. Pick a routine Windows actually runs hot. Disassemble the shipped version (`dumpbin /disasm`) and read
   what it does — baseline to beat, and the behavior contract to match exactly.
2. Write the plain scalar reference (`reference.c`) — the oracle.
3. Write `impl.asm` — hand-tuned for the bench ISA, with runtime CPUID dispatch if it's meant to travel.
4. Prove bit-exact vs reference **and** behavior-identical vs the live system function.
5. Benchmark vs the live system function across size classes, pinned core, controlled for the bad RAM.
6. Only a clean win on every size class lands. Record the numbers and the compared disassembly.

Where useful, a second agent (Codex / Gemini) proposes an independent implementation and the two are
judged by the same test+benchmark harness — the tests decide, not opinion.

---

## Live substitution

The landed functions don't just win a benchmark — [`live-substitution/`](live-substitution/) hot-patches
the real `ucrtbase.dll` exports in a running process so calls to them execute our assembly, proves the
results stay identical across a fuzz corpus (with a counter confirming our code ran), then reverts
cleanly. Per-process, runtime, reversible — not a global on-disk DLL swap.

Nine harnesses now cover **59 distinct exports** across `ucrtbase`, `ntdll`, `shlwapi` and `kernelbase`:
the compares, the integer formatters, a transform that writes an upcased output string through the
OS-built case-fold table (`RtlUpcaseUnicodeString`), the Zen-4 variants, the nine `shlwapi`/`kernelbase`
path routines, the bounded `_s` case-fold family (178–181), the bounded `_s` fill family (182–185),
the wide integer parsers (186–191, eight exported names) the two ntdll routines 192–193 and the bounded 64-bit formatter family (194–197).
The `_s` harnesses must install the invalid-parameter handler through **ucrtbase's own** setter and build
`/MD`, or the live export `__fastfail`s the process; the fill harness compares the **whole buffer** on every
case, because that family's error path writes; the wide-parser harness compares **value, `*endptr`
and `errno`** on a corpus deliberately weighted onto the derived character tables, so what it proves
is not only that our assembly is fast but that those tables are the ones ucrtbase actually uses.
The ntdll harness does the same for change 193, whose contract was READ OUT OF THE SHIPPED
DISASSEMBLY rather than derived black-box: it compares the BOOL and the rewritten `*lpi` on every
case, across a corpus weighted onto the two slots that were misread on the first pass.
See its RESULTS.md.

## Status

Foundation up; measurement pipeline proven end-to-end. Landed changes:

| # | routine | vs system | result |
|---|---|---|---|
| [001](changes/001-wcslen/) | `wcslen` (AVX2) | `ucrtbase.dll!wcslen` | **LANDED** — geomean **2.15×**, no regression |
| [002](changes/002-memchr/) | `memchr` (AVX2) | `ucrtbase.dll!memchr` | **LANDED** — geomean **2.28×**, up to 3.5× |
| [003](changes/003-wcschr/) | `wcschr` (AVX2) | `ucrtbase.dll!wcschr` | **LANDED** — geomean **2.19×**, up to 3.9× |
| [004](changes/004-wcscmp/) | `wcscmp` (AVX2) | `ucrtbase.dll!wcscmp` | **LANDED** — geomean **2.88×**, up to 4.2× |
| [005](changes/005-memcmp/) | `memcmp` (AVX2) | `ucrtbase.dll!memcmp` | **PARKED** — 1.8–2× ≥ 1 KB, but loses at ≤ 32 B |
| [006](changes/006-crc32/) | `crc32` (VPCLMULQDQ) | `ntdll.dll!RtlComputeCrc32` | **PARKED** — correct; ties ntdll (already PCLMUL-optimal) |
| [007](changes/007-rtlcomparememory/) | `RtlCompareMemory` (AVX2) | `ntdll.dll!RtlCompareMemory` | **LANDED** — geomean **4.42×**, 3–5× (core ntdll) |
| [008](changes/008-rtlcompareunicodestring/) | `RtlCompareUnicodeString` (AVX2) | `ntdll.dll!RtlCompareUnicodeString` | **LANDED** — geomean **3.97×**, CI up to 6.4× (core ntdll) |
| [009](changes/009-rtlhashunicodestring/) | `RtlHashUnicodeString` (AVX2) | `ntdll.dll!RtlHashUnicodeString` | **LANDED** — geomean **4.50×**, up to 8.2× (core ntdll) |
| [010](changes/010-rtlequalunicodestring/) | `RtlEqualUnicodeString` (AVX2) | `ntdll.dll!RtlEqualUnicodeString` | **LANDED** — geomean **1.96×** (beats even ntdll's fast cs path) |
| [011](changes/011-rtlprefixunicodestring/) | `RtlPrefixUnicodeString` (AVX2) | `ntdll.dll!RtlPrefixUnicodeString` | **LANDED** — geomean **1.97×**, CI up to 3.5× (core ntdll) |
| [012](changes/012-rtlcomparestring/) | `RtlCompareString` (ANSI, AVX2) | `ntdll.dll!RtlCompareString` | **LANDED** — geomean **4.47×**, CI up to 10× (core ntdll) |
| [013](changes/013-rtlequalstring/) | `RtlEqualString` (ANSI, AVX2) | `ntdll.dll!RtlEqualString` | **LANDED** — geomean **4.02×**, CI up to 7.4× (core ntdll) |
| [014](changes/014-rtlprefixstring/) | `RtlPrefixString` (ANSI, AVX2) | `ntdll.dll!RtlPrefixString` | **LANDED** — geomean **2.88×**, CI up to 11.4× (core ntdll) |
| [015](changes/015-rtlupcaseunicodestring/) | `RtlUpcaseUnicodeString` (AVX2) | `ntdll.dll!RtlUpcaseUnicodeString` | **LANDED** — geomean **9.12×**, up to 15.7× (core ntdll) |
| [016](changes/016-rtlunicodetoutf8n/) | `RtlUnicodeToUTF8N` (AVX2) | `ntdll.dll!RtlUnicodeToUTF8N` | **LANDED** — geomean **2.82×** (UTF-16→UTF-8, core ntdll) |
| [017](changes/017-rtldowncaseunicodestring/) | `RtlDowncaseUnicodeString` (AVX2) | `ntdll.dll!RtlDowncaseUnicodeString` | **LANDED** — geomean **11.15×**, up to 21× (core ntdll) |
| [018](changes/018-rtlunicodestringtoansistring/) | `RtlUnicodeStringToAnsiString` (AVX2) | `ntdll.dll!RtlUnicodeStringToAnsiString` | **LANDED** — geomean **12.79×**, up to 22.6× (core ntdll) |
| [019](changes/019-rtlansistringtounicodestring/) | `RtlAnsiStringToUnicodeString` (AVX2) | `ntdll.dll!RtlAnsiStringToUnicodeString` | **LANDED** — geomean **11.58×**, up to 22.9× (core ntdll) |
| [020](changes/020-rtlupcaseunicodestringtoansistring/) | `RtlUpcaseUnicodeStringToAnsiString` (AVX2) | `ntdll.dll!RtlUpcaseUnicodeStringToAnsiString` | **LANDED** — geomean **9.0×**, up to 13× (core ntdll) |
| [021](changes/021-rtlunicodetomultibyten/) | `RtlUnicodeToMultiByteN` (AVX2) | `ntdll.dll!RtlUnicodeToMultiByteN` | **LANDED** — geomean **3.93×** (core ntdll) |
| [022](changes/022-rtlmultibytetounicoden/) | `RtlMultiByteToUnicodeN` (AVX2) | `ntdll.dll!RtlMultiByteToUnicodeN` | **LANDED** — geomean **4.05×** (core ntdll) |
| [023](changes/023-rtlnumberofsetbits/) | `RtlNumberOfSetBits` (POPCNT) | `ntdll.dll!RtlNumberOfSetBits` | **LANDED** — geomean **1.32×**, 2.3× small (core ntdll) |
| [024](changes/024-rtlunicodestringtooemstring/) | `RtlUnicodeStringToOemString` (AVX2) | `ntdll.dll!RtlUnicodeStringToOemString` | **LANDED** — geomean **7.68×** (core ntdll) |
| [025](changes/025-rtloemstringtounicodestring/) | `RtlOemStringToUnicodeString` (AVX2) | `ntdll.dll!RtlOemStringToUnicodeString` | **LANDED** — geomean **5.50×** (core ntdll) |
| [026](changes/026-rtlcomparememoryulong/) | `RtlCompareMemoryUlong` (AVX2) | `ntdll.dll!RtlCompareMemoryUlong` | **LANDED** — geomean **5.04×** (core ntdll) |
| [027](changes/027-rtlupcaseunicodetomultibyten/) | `RtlUpcaseUnicodeToMultiByteN` (AVX2) | `ntdll.dll!RtlUpcaseUnicodeToMultiByteN` | **LANDED** — geomean **6.54×** (core ntdll) |
| [028](changes/028-rtlunicodetooemn/) | `RtlUnicodeToOemN` (AVX2) | `ntdll.dll!RtlUnicodeToOemN` | **LANDED** — geomean **6.34×** (core ntdll) |
| [029](changes/029-rtloemtounicoden/) | `RtlOemToUnicodeN` (AVX2) | `ntdll.dll!RtlOemToUnicodeN` | **LANDED** — geomean **4.51×** (core ntdll) |
| [030](changes/030-rtlarebitsset/) | `RtlAreBitsSet` (AVX2) | `ntdll.dll!RtlAreBitsSet` | **LANDED** — geomean **3.17×**, up to 7× (core ntdll) |
| [031](changes/031-rtlupcaseunicodetooemn/) | `RtlUpcaseUnicodeToOemN` (AVX2) | `ntdll.dll!RtlUpcaseUnicodeToOemN` | **LANDED** — geomean **7.41×** (core ntdll) |
| [032](changes/032-strlen/) | `strlen` (AVX2) | `ucrtbase.dll!strlen` | **LANDED** — geomean **2.83×**, up to 6.8× (ucrtbase) |
| [033](changes/033-strcmp/) | `strcmp` (AVX2) | `ucrtbase.dll!strcmp` | **LANDED** — geomean **1.31×** (ucrtbase) |
| [034](changes/034-rtlutf8tounicoden/) | `RtlUTF8ToUnicodeN` (AVX2) | `ntdll.dll!RtlUTF8ToUnicodeN` | **LANDED** — geomean **3.12×** (UTF-8→UTF-16 decoder, core ntdll) |
| [035](changes/035-wcspbrk/) | `wcspbrk` (AVX2 set-broadcast) | `ucrtbase.dll!wcspbrk` | **LANDED** — geomean **7.98×** (2.11×–13.6×; set hoisted into registers, spare slots holding a duplicate of member 0 since `a OR a == a`) |
| [036](changes/036-wcsspn/) | `wcsspn` (AVX2 set-broadcast) | `ucrtbase.dll!wcsspn` | **LANDED** — geomean **8.64×** (2.69×–13.96×; set hoisted out of the block loop, which also removed a code-layout hazard) |
| [037](changes/037-wcscspn/) | `wcscspn` (AVX2 set-broadcast) | `ucrtbase.dll!wcscspn` | **LANDED** — geomean **8.61×** (2.36×–13.95×; the NUL must be OR-ed into the stop mask, which the plain span gets for free) |
| [038](changes/038-strpbrk/) | `strpbrk` (AVX2 set-broadcast) | `ucrtbase.dll!strpbrk` | **LANDED** — geomean **4.98×** (1.85×–8.22×; hits and terminator in separate masks so a member one byte past the terminator still returns NULL) |
| [039](changes/039-strspn/) | `strspn` (AVX2 set-broadcast) | `ucrtbase.dll!strspn` | **LANDED** — geomean **6.98×** (2.44×–11.93×; unlike the wide version the live one already bitmaps the set, so the win is 32 chars per step) |
| [040](changes/040-strcspn/) | `strcspn` (AVX2 set-broadcast) | `ucrtbase.dll!strcspn` | **LANDED** — geomean **5.85×** (1.95×–10.42×; completes the byte tokenizer trio) |
| [041](changes/041-wcsncmp/) | `wcsncmp` (AVX2 bounded compare) | `ucrtbase.dll!wcsncmp` | **LANDED** — geomean **3.33×** (1.23×–5×, ~26 GB/s; bounded UTF-16 compare) |
| [042](changes/042-wcsicmp/) | `_wcsicmp` (AVX2 case-insensitive) | `ucrtbase.dll!_wcsicmp` | **LANDED** — geomean **7.66×** (2.6×–11.5×, ~26 GB/s; case-insensitive UTF-16 compare) |
| [043](changes/043-stricmp/) | `_stricmp` (AVX2 case-insensitive) | `ucrtbase.dll!_stricmp` | **LANDED** — geomean **8.73×** (2.3×–16×; case-insensitive byte compare) |
| [044](changes/044-wcsnicmp/) | `_wcsnicmp` (AVX2 ci-bounded) | `ucrtbase.dll!_wcsnicmp` | **LANDED** — geomean **7.44×** (2.5×–10.9×; bounded ci UTF-16 compare) |
| [045](changes/045-strnicmp/) | `_strnicmp` (AVX2 ci-bounded) | `ucrtbase.dll!_strnicmp` | **LANDED** — geomean **9.31×** (2.2×–17×; completes the ci compare family) |
| [046](changes/046-memicmp/) | `_memicmp` (AVX2 case-insensitive) | `ucrtbase.dll!_memicmp` | **LANDED** — geomean **10.70×** (2.2×–23.7×, ~35 GB/s; ci memory compare) |
| [047](changes/047-strlwr/) | `_strlwr` (AVX2 fold+store) | `ucrtbase.dll!_strlwr` | **LANDED** — geomean **8.80×** (1×–24.9×; in-place ASCII lowercase) |
| [048](changes/048-strupr/) | `_strupr` (AVX2 fold+store) | `ucrtbase.dll!_strupr` | **LANDED** — geomean **8.94×** (1.1×–25×; in-place ASCII uppercase) |
| [049](changes/049-wcslwr/) | `_wcslwr` (AVX2 fold+store) | `ucrtbase.dll!_wcslwr` | **PARKED** — correct + 2–6× ≥ 32, but ucrtbase's tight 8-wchar path wins at size 8 |
| [050](changes/050-wcsupr/) | `_wcsupr` (AVX2 fold+store) | `ucrtbase.dll!_wcsupr` | **LANDED** — geomean **5.98×** (1×–11.8×; in-place ASCII uppercase, wide) |
| [051](changes/051-rtlfindcharinunicodestring/) | `RtlFindCharInUnicodeString` (AVX2 set-search) | `ntdll.dll!RtlFindCharInUnicodeString` | **LANDED** — geomean **8.26×** (2.5×–13.3×; counted set-search, all 8 flags, core ntdll path parsing) |
| [052](changes/052-rtlintegertounicodestring/) | `RtlIntegerToUnicodeString` (2-digit table) | `ntdll.dll!RtlIntegerToUnicodeString` | **LANDED** — geomean **2.94×** (2.6×–3.3×; integer→decimal/hex format, new family) |
| [053](changes/053-rtlint64tounicodestring/) | `RtlInt64ToUnicodeString` (2-digit table) | `ntdll.dll!RtlInt64ToUnicodeString` | **LANDED** — geomean **3.10×** (2.9×–3.3×; 64-bit integer format) |
| [054](changes/054-ultoa/) | `_ultoa` (2-digit table / nibble) | `ucrtbase.dll!_ultoa` | **LANDED** — geomean **1.50×** (radix 2–36, C-runtime integer format) |
| [055](changes/055-ui64toa/) | `_ui64toa` (2-digit table / nibble) | `ucrtbase.dll!_ui64toa` | **LANDED** — geomean **1.56×** (64-bit C-runtime integer format) |
| [056](changes/056-itoa/) | `_itoa` (2-digit table / nibble) | `ucrtbase.dll!_itoa` | **LANDED** — geomean **1.44×** (signed C-runtime integer format) |
| [057](changes/057-i64toa/) | `_i64toa` (2-digit table / nibble) | `ucrtbase.dll!_i64toa` | **LANDED** — geomean **1.61×** (signed 64-bit integer format) |
| [058](changes/058-rtlstringfromguidex/) | `RtlStringFromGUIDEx` (table byte→hex) | `ntdll.dll!RtlStringFromGUIDEx` | **LANDED** — **25.5×** (GUID→string; COM/registry/ETW; project's largest win) |
| [059](changes/059-rtlipv4addresstostringa/) | `RtlIpv4AddressToStringA` (table) | `ntdll.dll!RtlIpv4AddressToStringA` | **LANDED** — **16.6×** (IPv4→dotted decimal; networking) |
| [060](changes/060-rtlethernetaddresstostringa/) | `RtlEthernetAddressToStringA` (table) | `ntdll.dll!RtlEthernetAddressToStringA` | **LANDED** — **36.5×** (MAC→string; project's largest ratio) |
| [061](changes/061-rtlipv4addresstostringw/) | `RtlIpv4AddressToStringW` (table) | `ntdll.dll!RtlIpv4AddressToStringW` | **LANDED** — **23.7×** (IPv4→dotted decimal, wide) |
| [062](changes/062-rtlethernetaddresstostringw/) | `RtlEthernetAddressToStringW` (table) | `ntdll.dll!RtlEthernetAddressToStringW` | **LANDED** — **36.0×** (MAC→string, wide) |
| [063](changes/063-rtlipv6addresstostringa/) | `RtlIpv6AddressToStringA` (state machine) | `ntdll.dll!RtlIpv6AddressToStringA` | **LANDED** — **5.92×** (IPv6→string; RFC 5952 + Windows; validated 3M) |
| [064](changes/064-rtlipv6addresstostringw/) | `RtlIpv6AddressToStringW` (state machine) | `ntdll.dll!RtlIpv6AddressToStringW` | **LANDED** — **7.42×** (IPv6→string, wide) |
| [065](changes/065-rtlipv4addresstostringexa/) | `RtlIpv4AddressToStringExA` (table) | `ntdll.dll!RtlIpv4AddressToStringExA` | **LANDED** — **9.78×** (IPv4+port→string) |
| [066](changes/066-rtlipv4addresstostringexw/) | `RtlIpv4AddressToStringExW` (table) | `ntdll.dll!RtlIpv4AddressToStringExW` | **LANDED** — **10.5×** (IPv4+port→string, wide) |
| [067](changes/067-rtlconvertsidtounicodestring/) | `RtlConvertSidToUnicodeString` (state machine) | `ntdll.dll!RtlConvertSidToUnicodeString` | **LANDED** — **1.42×** (SID→string; security/ACLs; validated 2M) |
| [068](changes/068-rtlipv6addresstostringexa/) | `RtlIpv6AddressToStringExA` (reuses 063 core) | `ntdll.dll!RtlIpv6AddressToStringExA` | **LANDED** — **6.87×** (IPv6+scope+port→string) |
| [069](changes/069-rtlipv6addresstostringexw/) | `RtlIpv6AddressToStringExW` (reuses 064 core) | `ntdll.dll!RtlIpv6AddressToStringExW` | **LANDED** — **8.38×** (IPv6+scope+port→string, wide) |
| [070](changes/070-strrev/) | `_strrev` (32-byte block swap + unrolled probe) | `ucrtbase.dll!_strrev` | **LANDED** — **9.89×** geomean (1.40×–33.55×; the final block pair is allowed to overlap — both stores provably write the same bytes there) |
| [071](changes/071-wcsrev/) | `_wcsrev` (word-granular 32-byte block swap + unrolled probe) | `ucrtbase.dll!_wcsrev` | **LANDED** — **6.60×** geomean (1.30×–17.31×; same overlap proof at word granularity) |
| [072](changes/072-ultow/) | `_ultow` (2-digit word table / nibble) | `ucrtbase.dll!_ultow` | **LANDED** — **1.55×** (unsigned 32-bit → wide string, radix 2–36) |
| [073](changes/073-ui64tow/) | `_ui64tow` (64-bit 2-digit word table) | `ucrtbase.dll!_ui64tow` | **LANDED** — **1.60×** (unsigned 64-bit → wide string, radix 2–36) |
| [074](changes/074-itow/) | `_itow` / `_ltow` (signed, 2-digit word table) | `ucrtbase.dll!_itow` | **LANDED** — **1.42×** (signed 32-bit → wide string; `_itow`≡`_ltow`) |
| [075](changes/075-i64tow/) | `_i64tow` (signed 64-bit) | `ucrtbase.dll!_i64tow` | **LANDED** — **1.59×** (signed 64-bit → wide string, radix 2–36) |
| [076](changes/076-rtlcrc64/) | `RtlCrc64` (slicing-by-8 + 256-bit VPCLMULQDQ fold; **RE'd**) | `ntdll.dll!RtlCrc64` | **LANDED** — **3.29×** (2.0–4.5×; reflected CRC-64 reverse-engineered, poly `0x9A6C9329AC4BC9B5`, `~init`/`~out`, validated 400k; **~21 GB/s** vs ntdll's 4.8 at size) |
| [077](changes/077-strset/) | `_strset` (AVX2 broadcast-fill) | `ucrtbase.dll!_strset` | **LANDED** — **4.58×** (1.21×–14.24×; fill string with a char) |
| [078](changes/078-strnset/) | `_strnset` (AVX2 broadcast + 8-byte tail) | `ucrtbase.dll!_strnset` | **LANDED** — **5.72×** (1.25×–13.77×; bounded fill) |
| [079](changes/079-wcsset/) | `_wcsset` (AVX2 word broadcast-fill) | `ucrtbase.dll!_wcsset` | **LANDED** — **3.68×** (1.12×–7.02×; fill wide string with a wchar) |
| [080](changes/080-wcsnset/) | `_wcsnset` (AVX2 word broadcast + 4-wchar tail) | `ucrtbase.dll!_wcsnset` | **LANDED** — **6.45×** (1.44×–12.50×; bounded wide fill) |
| [081](changes/081-cryptbinarytostring-base64/) | `CryptBinaryToStringA` base64 (SSSE3 Muła) | `crypt32.dll!CryptBinaryToStringA` | **LANDED** — **24.5×** (8.8×–36×; binary→base64, crypt32's is scalar ~0.32 GB/s) |
| [082](changes/082-cryptstringtobinary-base64/) | `CryptStringToBinaryA` base64 decode (SSSE3 Muła) | `crypt32.dll!CryptStringToBinaryA` | **LANDED** — **36.3×** (11.7×–56×; base64→binary; crypt32's is scalar ~0.13 GB/s) |
| [083](changes/083-cryptbinarytostringw-base64/) | `CryptBinaryToStringW` wide base64 (SSSE3 + widen) | `crypt32.dll!CryptBinaryToStringW` | **LANDED** — **8.9×** (8.3×–9.2×; wide binary→base64) |
| [084](changes/084-cryptstringtobinaryw-base64/) | `CryptStringToBinaryW` wide base64 decode (SSSE3) | `crypt32.dll!CryptStringToBinaryW` | **LANDED** — **38.7×** (13.7×–59×; wide base64→binary) |
| [085](changes/085-cryptbinarytostring-hexraw/) | `CryptBinaryToStringA` HEXRAW (SSSE3 nibble-LUT) | `crypt32.dll!CryptBinaryToStringA` | **LANDED** — **920×** (230×–1655×; binary→hex; **the project's largest win** — crypt32's hex is ~0.01 GB/s) |
| [086](changes/086-cryptstringtobinary-hexraw/) | `CryptStringToBinaryA` HEXRAW decode (SSSE3) | `crypt32.dll!CryptStringToBinaryA` | **LANDED** — **259×** (125×–335×; hex→binary; crypt32's hex decode is ~0.017 GB/s) |
| [087](changes/087-cryptbinarytostringw-hexraw/) | `CryptBinaryToStringW` wide HEXRAW (SSSE3 + widen) | `crypt32.dll!CryptBinaryToStringW` | **LANDED** — **133×** (wide binary→hex) |
| [088](changes/088-cryptstringtobinaryw-hexraw/) | `CryptStringToBinaryW` wide HEXRAW decode (SSSE3) | `crypt32.dll!CryptStringToBinaryW` | **LANDED** — **248×** (wide hex→binary) |
| [089](changes/089-strstr/) | `strstr` byte substring (AVX2 two-char anchor) | `ucrtbase.dll!strstr` | **PARKED** — ucrtbase is SSE4.2 `pcmpistri`; we win only ≥4 KB (1.27×), lose below ~2 KB |
| [090](changes/090-cryptbinarytostring-hexfmt/) | `CryptBinaryToStringA` formatted hex — HEX/HEXASCII/HEXADDR/HEXASCIIADDR (SSSE3 hex core + pshufb space-splice + SIMD ASCII clamp) | `crypt32.dll!CryptBinaryToStringA` | **LANDED** — **150×** (117×–169×; binary→formatted hexdump; crypt32's is scalar ~0.011 GB/s) |
| [091](changes/091-cryptbinarytostringw-hexfmt/) | `CryptBinaryToStringW` wide formatted hex, all 4 modes (SSSE3 core + reverse widen) | `crypt32.dll!CryptBinaryToStringW` | **LANDED** — **77×** (35×–465×; wide formatted hexdump) |
| [092](changes/092-cryptbinarytostring-base64header/) | `CryptBinaryToStringA` PEM-header base64 — BASE64HEADER/REQUESTHEADER/X509CRLHEADER (081 base64 core + BEGIN/END wrapper) | `crypt32.dll!CryptBinaryToStringA` | **LANDED** — **16.1×** (4.3×–30×; binary→PEM cert/CRL/request) |
| [093](changes/093-cryptbinarytostringw-base64header/) | `CryptBinaryToStringW` wide PEM-header base64, 3 modes (SSSE3 core + reverse widen) | `crypt32.dll!CryptBinaryToStringW` | **LANDED** — **57×** (9.9×–348×; wide PEM output) |
| [094](changes/094-rtlinitunicodestring/) | `RtlInitUnicodeString` (inline AVX2 wcslen + struct fill, no `call`) | `ntdll.dll!RtlInitUnicodeString` | **LANDED** — **1.76×** (1.18×–2.18×; called on every UNICODE_STRING setup) |
| [095](changes/095-rtlinitstring/) | `RtlInitString` = `RtlInitAnsiString` (inline AVX2 strlen + struct fill) | `ntdll.dll!RtlInitString` | **LANDED** — **1.43×** (1.12×–3.04×; ANSI_STRING setup) |
| [096](changes/096-rtlinitunicodestringex/) | `RtlInitUnicodeStringEx` (inline AVX2 wcslen + validate, NTSTATUS) | `ntdll.dll!RtlInitUnicodeStringEx` | **LANDED** — **1.40×** (1.05×–2.02×; validating UNICODE_STRING setup) |
| [097](changes/097-rtlintegertochar/) | `RtlIntegerToChar` (2-digit decimal table; direct MSB-first hex/binary) | `ntdll.dll!RtlIntegerToChar` | **LANDED** — **1.39×** (1.11×–2.05×; ANSI integer→string, all bases) |
| [098](changes/098-rtlinitstringex/) | `RtlInitStringEx` (inline AVX2 strlen + validate, NTSTATUS) | `ntdll.dll!RtlInitStringEx` | **LANDED** — **1.37×** (1.04×–2.85×; validating ANSI_STRING setup) |
| [099](changes/099-strncmp/) | `strncmp` byte bounded compare (AVX2 two-regime) | `ucrtbase.dll!strncmp` | **PARKED** — ucrtbase is tuned aligned SWAR; we win ≥128 B (up to 2.06×) but lose the 32 B class |
| [100](changes/100-rtllargeintegertochar/) | `RtlLargeIntegerToChar` (64-bit; 2-digit decimal table + direct hex/binary) | `ntdll.dll!RtlLargeIntegerToChar` | **LANDED** — **1.43×** (1.14×–1.81×; 64-bit integer→string, all bases) |
| [101](changes/101-rtlappendunicodetostring/) | `RtlAppendUnicodeToString` (inline AVX2 wcslen + SIMD copy, no calls) | `ntdll.dll!RtlAppendUnicodeToString` | **LANDED** — **1.42×** (1.00×–2.34×; UNICODE_STRING builder) |
| [102](changes/102-rtlappendunicodestringtostring/) | `RtlAppendUnicodeStringToString` (counted append) | `ntdll.dll!RtlAppendUnicodeStringToString` | **PARKED** — no wcslen to cut; ntdll already optimal |
| [103](changes/103-rtlappendasciiztostring/) | `RtlAppendAsciizToString` (inline AVX2 strlen + copy) | `ntdll.dll!RtlAppendAsciizToString` | **PARKED** — ntdll's ANSI strlen already fast; at parity |
| [104](changes/104-cryptstringtobinary-base64header/) | `CryptStringToBinaryA` PEM decode — BASE64HEADER (scalar table decode + header scan) | `crypt32.dll!CryptStringToBinaryA` | **LANDED** — **11.0×** (3.9×–17.3×; PEM→binary, SSSE3 core) |
| [105](changes/105-cryptstringtobinaryw-base64header/) | `CryptStringToBinaryW` wide PEM decode — BASE64HEADER (per-wchar scan/decode) | `crypt32.dll!CryptStringToBinaryW` | **LANDED** — **11.7×** (4.9×–17.0×; wide PEM→binary, SSSE3+packuswb) |
| [106](changes/106-cryptstringtobinary-base64any/) | `CryptStringToBinaryA` BASE64_ANY decode (auto-detect PEM vs plain base64) | `crypt32.dll!CryptStringToBinaryA` | **LANDED** — **5.3×** (4.0×–5.8×; SSSE3 core, sets pdwFlags 0/1) |
| [107](changes/107-cryptstringtobinaryw-base64any/) | `CryptStringToBinaryW` wide BASE64_ANY decode (auto-detect) | `crypt32.dll!CryptStringToBinaryW` | **LANDED** — **5.8×** (5.2×–6.0×; wide SSSE3+packuswb, sets pdwFlags 0/1) |
| [108](changes/108-atoi/) | `atoi` (frameless scalar; skip ws, sign, saturating decimal) | `ucrtbase.dll!atoi` | **LANDED** — **2.11×** (2.0×–2.4×; parse-side of the itoa family; no CRT/locale) |
| [109](changes/109-atoi64/) | `_atoi64` (64-bit saturating scalar parse) | `ucrtbase.dll!_atoi64` | **LANDED** — **2.20×** (2.1×–2.5×; ±2⁶³ saturation; `_wtoi` scoped out — Unicode digits) |
| [110](changes/110-strtol/) | `strtol` (base 0/2–36, endptr, ERANGE; 256-entry digit table) | `ucrtbase.dll!strtol` | **LANDED** — **2.23×** (2.1×–2.4×; value+endptr+errno bit-exact; reference-first) |
| [111](changes/111-strtoul/) | `strtoul` (unsigned; `-` negates mod 2³²; ERANGE) | `ucrtbase.dll!strtoul` | **LANDED** — **1.97×** (1.8×–2.2×; value+endptr+errno bit-exact) |
| [112](changes/112-strtoi64/) | `_strtoi64` (64-bit signed; mul-carry overflow guard) | `ucrtbase.dll!_strtoi64` | **LANDED** — **1.79×** (1.7×–2.0×; ±2⁶³ saturation; value+endptr+errno) |
| [113](changes/113-strtoui64/) | `_strtoui64` (64-bit unsigned; `-` negates mod 2⁶⁴) | `ucrtbase.dll!_strtoui64` | **LANDED** — **1.76×** (1.6×–1.9×; value+endptr+errno bit-exact) |
| [114](changes/114-rtlipv4stringtoaddress/) | `RtlIpv4StringToAddressA` (inet_aton IPv4 parse; parse-side of 059) | `ntdll.dll!RtlIpv4StringToAddressA` | **LANDED** — **1.53×** (1.3×–1.8×; STATUS+addr+Terminator bit-exact; reference-first 7-pass RE) |
| [115](changes/115-rtlipv4stringtoaddressw/) | `RtlIpv4StringToAddressW` (wide inet_aton IPv4 parse) | `ntdll.dll!RtlIpv4StringToAddressW` | **LANDED** — **1.51×** (1.4×–1.7×; WCHAR≥0x100→terminator; bit-exact incl. non-ASCII) |
| [116](changes/116-rtlipv4stringtoaddressex/) | `RtlIpv4StringToAddressExA` (IPv4 + `:port`, whole-string) | `ntdll.dll!RtlIpv4StringToAddressExA` | **LANDED** — **1.96×** (1.7×–2.2×; STATUS+addr+net-order port bit-exact) |
| [117](changes/117-rtlipv4stringtoaddressexw/) | `RtlIpv4StringToAddressExW` (wide IPv4 + `:port`) | `ntdll.dll!RtlIpv4StringToAddressExW` | **LANDED** — **1.80×** (1.6×–2.0×; completes IPv4 parser family A/W/Ex A/W) |
| [118](changes/118-rtlguidfromstring/) | `RtlGUIDFromString` (`{...}` GUID parse; parse-side of 058) | `ntdll.dll!RtlGUIDFromString` | **LANDED** — **4.45×** (12 vs 53 ns; fixed-offset hex parse, frameless) |
| [119](changes/119-rtlethernetstringtoaddress/) | `RtlEthernetStringToAddressA` (MAC parse; parse-side of 060) | `ntdll.dll!RtlEthernetStringToAddressA` | **LANDED** — **6.29×** (11 vs 70 ns; STATUS+addr+Terminator bit-exact) |
| [120](changes/120-rtlethernetstringtoaddressw/) | `RtlEthernetStringToAddressW` (wide MAC parse) | `ntdll.dll!RtlEthernetStringToAddressW` | **LANDED** — **4.17×** (11 vs 44 ns; WCHAR≥0x100 handling; bit-exact) |
| [121](changes/121-rtlipv6stringtoaddress/) | `RtlIpv6StringToAddressA` (IPv6 parse: `::`, embedded IPv4; parse-side of 063) | `ntdll.dll!RtlIpv6StringToAddressA` | **LANDED** — **4.66×** (up to **10.03×**; 6M-fuzz bit-exact. **Corrected while building [166]** — four defects on paths the old fuzz could not generate, incl. that the stored value is a *re-parse of the token* honouring a `0x` prefix, not what the scan accumulated) |
| [122](changes/122-rtlipv6stringtoaddressex/) | `RtlIpv6StringToAddressExA` (IPv6 `[addr%zone]:port`; wraps 121 core) | `ntdll.dll!RtlIpv6StringToAddressExA` | **LANDED** — **4.17×** (up to **7.5×**; brackets/`%scope`/`:port`; 2M-fuzz bit-exact; `ExW` scoped out — Unicode digits) |
| [123](changes/123-rtlfindlongestrunclear/) | `RtlFindLongestRunClear` (bitmap: longest run of clear bits) | `ntdll.dll!RtlFindLongestRunClear` | **LANDED** — **4.79×** (up to **30×**; word-scan + AVX2 all-zero skip; popcount-adaptive interior; 5M-fuzz bit-exact) |
| [124](changes/124-rtlnumberofclearbits/) | `RtlNumberOfClearBits` (bitmap: count clear bits; complement of 023) | `ntdll.dll!RtlNumberOfClearBits` | **LANDED** — **1.33×** (2.4× small; BW-bound parity at scale; masked POPCNT, `n − set`) |
| [125](changes/125-rtlfindclearbits/) | `RtlFindClearBits` (bitmap: find N consecutive clear bits, hint+wrap) | `ntdll.dll!RtlFindClearBits` | **PARKED** — bit-exact (5M fuzz); ntdll already byte-table optimized. Wins 2.65× only on huge contiguous-region scans (AVX2 skip); ties/loses found-fast |
| [126](changes/126-rtltimetotimefields/) | `RtlTimeToTimeFields` (FILETIME → Y/M/D h:m:s.ms + weekday) | `ntdll.dll!RtlTimeToTimeFields` | **LANDED** — **1.73×** (all divisions → verified `mulx` multiply-high; era-based civil-from-days, branch-free; bit-exact on every one of 10.67M days) |
| [127](changes/127-rtltimefieldstotime/) | `RtlTimeFieldsToTime` (Y/M/D h:m:s.ms → FILETIME; parse-side of 126) | `ntdll.dll!RtlTimeFieldsToTime` | **LANDED** — **1.54×** (era-based days-from-civil; unsigned-compare validation; year bound 1601–30827 RE'd; 800y sweep + 109k round-trips) |
| [128](changes/128-rtlsecondssince1970totime/) | `RtlSecondsSince1970ToTime` (Unix seconds → FILETIME) | `ntdll.dll!RtlSecondsSince1970ToTime` | **LANDED** — **2.00×** (4 instructions; epoch offset folded through the multiply; full ULONG-domain validation) |
| [129](changes/129-rtlchartointeger/) | `RtlCharToInteger` (ASCII string → ULONG, base 0/2/8/10/16) | `ntdll.dll!RtlCharToInteger` | **PARKED** — bit-exact (all 256 bytes × positions × bases + 3M fuzz); **1.88×** on the base-0 hex path but loses on 2-char inputs. Notable RE: **signed-char** whitespace skip (skips 0x80–0xFF), lowercase-only prefixes, leading `0` = decimal |
| [130](changes/130-rtlsetbits/) | `RtlSetBits` (bitmap: set a bit range) | `ntdll.dll!RtlSetBits` | **PARKED** — **1.36×** geomean, wins every size but one (**2.53×** to set 1 bit); 512-byte fill 0.84×. Size-dispatched fill (SSE2 / AVX2 / `rep stosd`) with each boundary measured; no bounds check in the contract |
| [131](changes/131-strchrw/) | `StrChrW` (first wchar match) — **first `shlwapi.dll` target** | `shlwapi.dll!StrChrW` | **LANDED** — **3.67×** (up to **6.32×**; shlwapi is still scalar at ~1.1 cyc/char; AVX2 dual-compare + page-safe aligned loads, NOACCESS-guard validated) |
| [132](changes/132-pathfindextensionw/) | `PathFindExtensionW` (path → extension pointer) | `shlwapi.dll!PathFindExtensionW` | **LANDED** — **5.73×** (up to **8.82×**; single-pass AVX2 with a per-block "backslash clears, later dot sets" rule; RE'd that only `\` stops the search — `/` and `:` do not) |
| [133](changes/133-strstrw/) | `StrStrW` (ordinal substring search) | `shlwapi.dll!StrStrW` | **LANDED** — **5.77×** (up to **12.66×**; AVX2 first-char scan + scalar verify, page-safe without an anchor; empty needle → NULL, unlike `wcsstr`) |
| [134](changes/134-strrchrw/) | `StrRChrW` (last wchar match, optionally bounded) | `shlwapi.dll!StrRChrW` | **LANDED** — **4.75×** (up to **6.48×**; backward AVX2 when bounded, forward last-match tracking when not; aligned loads ⇒ structural page safety) |
| [135](changes/135-strspnw/) | `StrSpnW` (initial run of chars from a set) | `shlwapi.dll!StrSpnW` | **LANDED** — **5.87×** (up to **12.12×**; shlwapi is naive O(n·m), this does 16 chars/compare; case-insensitive siblings scoped out as collation-based) |
| [136](changes/136-strcspnw/) | `StrCSpnW` (initial run of chars *not* in a set) | `shlwapi.dll!StrCSpnW` | **LANDED** — **11.26×** (up to **15.19×**; complement of 135 — here the NUL must be compared explicitly, since a set can never contain it) |
| [137](changes/137-strpbrkw/) | `StrPBrkW` (first char from a set) | `shlwapi.dll!StrPBrkW` | **LANDED** — **8.83×** (up to **12.85×**; separate hit/terminator masks so a miss returns NULL, not the terminator) |
| [138](changes/138-pathisfilespecw/) | `PathIsFileSpecW` (no path delimiter present?) | `shlwapi.dll!PathIsFileSpecW` | **LANDED** — **5.73×** (up to **7.15×**; RE'd that only `:` and `\` disqualify — `/` does not, a *third* separator convention in the same DLL) |
| [139](changes/139-strtrimw/) | `StrTrimW` (in-place trim of a char set from both ends) | `shlwapi.dll!StrTrimW` | **LANDED** — **17.24×** (up to **34.95×** — session best; AVX2 lead+length scans, scalar trailing scan by design; whole-buffer bit-exact incl. terminate-before-move ordering) |
| [140](changes/140-pathremoveextensionw/) | `PathRemoveExtensionW` (truncate at the extension) | `shlwapi.dll!PathRemoveExtensionW` | **LANDED** — **2.98×** (up to **8.27×**; reuses 132's scan, **plus a MAX_PATH guard 132 does not have** — ≥260 chars is a no-op, found by fuzzing) |
| [141](changes/141-pathremoveblanksw/) | `PathRemoveBlanksW` (strip leading/trailing spaces) | `shlwapi.dll!PathRemoveBlanksW` | **LANDED** — **4.23×** (up to **10.84×**; space-only, **no** MAX_PATH guard unlike 140, and move-then-terminate — the reverse of 139) |
| [142](changes/142-pathaddbackslashw/) | `PathAddBackslashW` (append a trailing backslash) | `shlwapi.dll!PathAddBackslashW` | **PARKED** — bit-exact; **3.13×** geomean (7.1× on real paths) but **0.85×** at 16 chars. RE'd the result-fits-in-260 rule *and its ordering* (long already-terminated paths return NULL) |
| [143](changes/143-pathcchfindextension/) | `PathCchFindExtension` — **first `kernelbase.dll` target** | `kernelbase.dll!PathCchFindExtension` | **LANDED** — **4.42×** (up to **6.35×**; the "safe" modern replacement is *slower* than the shlwapi one it replaces; same rule as 132 + a clipped `cchPath` bound) |
| [144](changes/144-pathcchremoveextension/) | `PathCchRemoveExtension` (truncate at the extension) | `kernelbase.dll!PathCchRemoveExtension` | **LANDED** — **2.59×** (up to **5.87×**; S_OK vs **S_FALSE**, and an extra length≤259 limit that 143 does *not* have — a 4th distinct MAX_PATH behaviour) |
| [145](changes/145-swab/) | `_swab` (swap adjacent byte pairs) | `ucrtbase.dll!_swab` | **LANDED** — **7.91×** (up to **20×**, ~90 GB/s; one `vpshufb` vs ucrtbase's ~1 cycle/byte — the routine left behind while the rest of ucrtbase got vectorised) |
| [146](changes/146-memccpy/) | `_memccpy` (copy until a delimiter byte) | `ucrtbase.dll!_memccpy` | **LANDED** — **8.69×** (up to **26.17×**, ~59 GB/s; fused memchr+memcpy in one AVX2 pass, with the delimiter block truncated byte-wise) |
| [147](changes/147-strtok-s/) | `strtok_s` (tokenizer) | `ucrtbase.dll!strtok_s` | **LANDED** — **5.43×** (up to **21.89×**; two scalar set-scans → two AVX2 block scans; validated by tokenising to exhaustion, comparing token+context+whole buffer each step) |
| [148](changes/148-wcstok-s/) | `wcstok_s` (wide tokenizer) | `ucrtbase.dll!wcstok_s` | **LANDED** — **6.14×** (up to **14.99×**; word-granular port of 147, validated with a low-byte-collision trap that a byte-wise compare would fail) |
| [149](changes/149-wcsrchr/) | `wcsrchr` (last wchar match) | `ucrtbase.dll!wcsrchr` | **LANDED** — **1.82×** (up to **2.31×**; the wide half left behind while `strrchr` was vectorised; forward last-match pass avoids a second scan for the terminator) |
| [150](changes/150-strcpy-s/) | `strcpy_s` (bounds-checked copy) | `ucrtbase.dll!strcpy_s` | **LANDED** — **3.86×** (up to **8.62×**; the `_s` variants are ~4× their plain counterparts — a scalar bounded byte loop. Error paths reproduced exactly by calling ucrtbase's own exported `_invalid_parameter_noinfo`, including the observable partial copy before `ERANGE`) |
| [151](changes/151-wcscpy-s/) | `wcscpy_s` (wide bounds-checked copy) | `ucrtbase.dll!wcscpy_s` | **LANDED** — **2.63×** (up to **3.81×**; wide port of 150, with a saturating wchar→byte bound conversion so an absurd `size` clamps instead of wrapping into a spurious `ERANGE`) |
| [152](changes/152-strcat-s/) | `strcat_s` (bounds-checked append) | `ucrtbase.dll!strcat_s` | **LANDED** — **4.57×** (up to **12.30×** appending to a 1000-char string; two scalar loops → two overlapped AVX2 scans, plus a scalar `dst[0]` test that dodges a 24-cycle store-forwarding stall on the `dst[0]=0; strcat_s(...)` idiom) |
| [153](changes/153-wcscat-s/) | `wcscat_s` (wide bounds-checked append) | `ucrtbase.dll!wcscat_s` | **LANDED** — **4.30×** (up to **8.19×**; wide port of 152 — overlapped scans, the scalar `dst[0]` store-forwarding dodge, and 151's saturating wchar→byte bound) |
| [154](changes/154-strncpy-s/) | `strncpy_s` (bounded copy with count) | `ucrtbase.dll!strncpy_s` | **LANDED** — **3.75×** (up to **8.66×**; seven contract paths collapsed to one `min(count,size)` scan, with `_TRUNCATE` terminating `dst[size-1]` silently where the counted form empties the string and raises `ERANGE`) |
| [155](changes/155-wcsncpy-s/) | `wcsncpy_s` (wide bounded copy with count) | `ucrtbase.dll!wcsncpy_s` | **LANDED** — **2.70×** (up to **4.36×**; `_TRUNCATE` is `(size_t)-1`, so only `min(count,size)` is doubled to bytes — doubling `count` itself would wrap the sentinel to `-2`) |
| [156](changes/156-strncat-s/) | `strncat_s` (bounded append with count) | `ucrtbase.dll!strncat_s` | **LANDED** — **4.35×** (up to **12.72×**; eight contract paths, three differing from `strncpy_s` — notably `count==0` with a NULL src writes nothing at all and skips the dst walk entirely) |
| [157](changes/157-wcsncat-s/) | `wcsncat_s` (wide bounded append with count) | `ucrtbase.dll!wcsncat_s` | **LANDED** — **2.85×** (up to **6.80×**; closes the bounded string family — `count` is doubled only after the `_TRUNCATE` branch, and the carry out of that shift doubles as the "cannot possibly fit" test) |
| [158](changes/158-pathrenameextensionw/) | `PathRenameExtensionW` | `shlwapi.dll!PathRenameExtensionW` | **LANDED** — **4.21×** (up to **6.47×**; reuses change 132 extension finder verbatim, so only the length arithmetic is new. The 259-char limit was pinned exactly and the buffer is left untouched on failure) |
| [159](changes/159-pathcchrenameextension/) | `PathCchRenameExtension` | `kernelbase.dll!PathCchRenameExtension` | **LANDED** — **3.94×** (up to **5.69×**; two distinct failure modes — `E_INVALIDARG` leaves the buffer untouched, `STRSAFE_E_INSUFFICIENT_BUFFER` writes exactly `cch-1` chars. Rejected-character set pinned by a full 65536-char sweep: only space, backslash and a non-leading dot) |
| [160](changes/160-pathcchaddextension/) | `PathCchAddExtension` | `kernelbase.dll!PathCchAddExtension` | **LANDED** — **3.80×** (up to **5.44×**; five outcomes with a measured precedence, and a truncating write on both size failures whose error code names whichever limit bound first, not how far over the result was) |
| [161](changes/161-pathfindfilenamew/) | `PathFindFileNameW` | `shlwapi.dll!PathFindFileNameW` | **LANDED** — **3.41×** (up to **10.61×**; the separator rule an earlier pass abandoned after four failed hypotheses, **derived** by constraint solving over 87k enumerated strings and verified exhaustively on 2.7M: a `:` separates only when it is the sole colon in its run) |
| [162](changes/162-pathstrippathw/) | `PathStripPathW` | `shlwapi.dll!PathStripPathW` | **LANDED** — **3.28×** (up to **8.91×**; unblocked by 161 — verified byte-for-byte equal to moving `PathFindFileNameW`s result to the front, and it leaves the stale tail past the new terminator untouched) |
| [163](changes/163-pathremovefilespecw/) | `PathRemoveFileSpecW` | `shlwapi.dll!PathRemoveFileSpecW` | **PARKED** — contract not fully derived. Established: it is **not** the Wine algorithm (`":"` disagrees), `/` is not a separator, the drive rule needs the colon at index 1 — but leading backslash runs are non-monotonic in length (k=3→3, k=4→2), so no single rule fits. Probes committed |
| [164](changes/164-pathcchaddbackslash/) | `PathCchAddBackslash` | `kernelbase.dll!PathCchAddBackslash` | **LANDED** — **2.17×** (up to **3.53×**; `S_FALSE` sits *between* the two size checks, and paths ≤23 chars never touch a vector register — a 2-byte load forwards from a caller`s recent write where a 32-byte load cannot) |
| [165](changes/165-rtlupperstring/) | `RtlUpperString` (ASCII case fold) | `ntdll.dll!RtlUpperString` | **LANDED** — **88.5×** (up to **364.85×**, the largest in this repo; ntdll pays a *call per byte* to `RtlUpperChar`. Safe to vectorise because `RtlUpperChar` was verified over all 256 values to be the plain ASCII rule — no locale table exists to reproduce) |
| [166](changes/166-rtlipv6stringtoaddressw/) | `RtlIpv6StringToAddressW` (wide IPv6 parse) | `ntdll.dll!RtlIpv6StringToAddressW` | **LANDED** — **3.62×** (up to **6.84×**; the form [121] had scoped out as needing "the CRT/OS Unicode digit table". Swept over all 65536 units the digit set is exactly **17 contiguous blocks of ten** — the frozen Unicode 3.0 `Nd` list. The scan is ASCII-only and fixes `seen`/`*Terminator`; the *value* is a separate re-parse of the token that honours `0x` and those blocks, and runs past where the scan stopped. Found and fixed **four defects in the landed 121**) |
| [167](changes/167-pathcommonprefixw/) | `PathCommonPrefixW` (longest common path prefix) | `shlwapi.dll!PathCommonPrefixW` | **PARKED** — contract not fully derived. The go/no-go passed: its case-fold is *exactly* `RtlUpcaseUnicodeChar` (0 differences over 65534 code-unit pairs, vs 947 for a plain ASCII fold), so unlike the `StrChrIW` family it is reachable. But leading-backslash / incomplete-UNC roots are non-monotonic (`\` vs `\`→0 while `\` vs `\\`→3), so 784 of 116281 exhaustive pairs resist any single rule. 99.3 % solved; five probe programs committed |
| [168](changes/168-strcpynw/) | `StrCpyNW` (bounded wide copy) | `shlwapi.dll!StrCpyNW` | **LANDED** — **5.01x** (up to 12.65x; fused scan-and-copy in ONE pass, no separate `wcslen`, so it also wins when the bound truncates. Contract: `strlcpy`-shaped not `strncpy`-shaped — no NUL fill — and `cchMax` is signed, so 0 or negative writes *nothing at all*. Confirmed first try, 2M fuzz, 0 mismatches) |
| [169](changes/169-strchrnw/) | `StrChrNW` (bounded wide character search) | `shlwapi.dll!StrChrNW` | **LANDED** — **3.48x** (up to 9.76x; AVX2 dual-compare locates the match and the terminator in the same pass, one `tzcnt` decides which came first. The NUL test must come FIRST — so `wMatch==0` is *always* NULL; the reverse order was refuted on 210697/3M cases) |
| [170](changes/170-strcatbuffw/) | `StrCatBuffW` (bounded wide append) | `shlwapi.dll!StrCatBuffW` | **LANDED** — **3.59x** (up to 9.87x; inline AVX2 `wcslen` + change 168's fused copy — it is exactly `StrCpyNW(dst+wcslen(dst), src, cch-wcslen(dst))`. A SWAR short-dst probe was required to land: the empty-dst class sat at 0.90x without it. No-room leaves dst byte-identical) |
| [171](changes/171-pathremovebackslashw/) | `PathRemoveBackslashW` (strip one trailing backslash) | `shlwapi.dll!PathRemoveBackslashW` | **LANDED** — **2.10x** (up to 5.81x; returns a pointer to the LAST CHARACTER, not the terminator — that off-by-one was refuted on 1451150/2M cases with otherwise-identical buffers. Protection is tested on the RESULT, making backslash runs non-monotonic. Drive-letter set pinned by an exhaustive 65535-character sweep to exactly the 114 ASCII + Latin-1 letters, excluding U+00D7 and U+00F7) |
| [172](changes/172-pathquotespacesw/) | `PathQuoteSpacesW` (wrap a path containing a space in quotes) | `shlwapi.dll!PathQuoteSpacesW` | **LANDED** — **2.06x** (up to 5.97x; ONE pass finds the length and the space together, with `tzcnt` of an empty mask (=32) deciding "space after terminator" for free. "Space" is EXACTLY U+0020 — 1 of 65535 code units, tab does not count — and the MAX_PATH rule is n<=257, both pinned by sweep. Landing it needed a size-laddered overlapping move AND narrow 16-byte probes, because a 32-byte aligned load cannot store-forward from the caller's recent narrow writes) |
| [173](changes/173-pathfindnextcomponentw/) | `PathFindNextComponentW` (next path component) | `shlwapi.dll!PathFindNextComponentW` | **LANDED** — **3.98x** (up to 8.05x, ~70 GB/s; ONE pass locates the separator and the terminator together. Two contract traps, both refuted the first candidate on 692689/2M: a DOUBLED separator skips exactly ONE extra character, never the whole run ("a\b" and "a\\b" both return index 3), and with NO separator it returns the TERMINATOR, not NULL. Separator swept to exactly U+005C — the sixth distinct separator convention found in this DLL) |
| [174](changes/174-pathundecoratew/) | `PathUndecorateW` (remove a `[n]` decoration) | `shlwapi.dll!PathUndecorateW` | **LANDED** — **3.72x** (up to 6.61x; ONE forward pass yields the length, the last backslash AND the last dot from three vpcmpeqw per block, using bsr for last-match and taking two broadcast constants as VEX MEMORY operands because only ymm0-ymm5 are volatile. The contract took three rounds: the group must hug the LAST dot, not merely be followed by one — "a[1].b[2].c" undecorates to "a[1].b.c", refuting the obvious rule on 171989 then 29053 of 2M cases. Empty brackets DO count; the '[' may not start the component) |
| [175](changes/175-pathremoveargsw/) | `PathRemoveArgsW` (strip command-line arguments) | `shlwapi.dll!PathRemoveArgsW` | **LANDED** — **6.34x** (up to 17.71x; an EVENT scan — one mask for {terminator, space, quote} so only real events drive the quote state machine, while skipping runs 16 chars/step. The contract needed a cell-level probe: it writes MORE THAN ONE cell ("ab   c" writes cells 2 AND 4, the LAST space of the run), and with no unquoted space it falls back to a TRAILING-BLANK TRIM — which is why `"`+space is cut but `"`+space+`a` is not. The obvious rule was refuted on 265039/2M) |
| [176](changes/176-pathcchremovebackslash/) | `PathCchRemoveBackslash` (bounded trailing-backslash strip) | `kernelbase.dll!PathCchRemoveBackslash` | **LANDED** — **1.93x** (up to 4.70x; change 171's core reused verbatim + an HRESULT and a BOUNDED length scan, where respecting cchPath is a CORRECTNESS requirement, not just safety — reading past it could find a terminator that must not count. Its drive-letter set is IDENTICAL to 171's across all 65535 code units, so two DLLs share one table. Unlike change 143's [1,32768], this one has NO upper bound on cchPath. Landed only after a narrow SWAR probe fixed 0.94x/0.83x on the short classes) |
| [177](changes/177-pathisprefixw/) | `PathIsPrefixW` (is one path a prefix of another) | `shlwapi.dll!PathIsPrefixW` | **PARKED** — blocked by change 167, and the blockage is PROVEN rather than assumed: `PathIsPrefixW(pre,path)` is exactly `PathCommonPrefixW(path,pre,NULL) == wcslen(pre)`, fuzz-verified against BOTH live exports (2M cases, 0 mismatches). That explains its 606 ns (it is paying for 167's work) and every odd probe result, including why a trailing backslash in the prefix returns FALSE. Solving 167 would unlock this one too — ~1.3 us of shipped scalar work between them |
| [178](changes/178-wcsupr-s/) | `_wcsupr_s` (bounded wide upcase) | `ucrtbase.dll!_wcsupr_s` | **LANDED** — **4.24x** (up to 9.38x; the `_s` sibling of change 050. Fold pinned by a 65535-unit sweep to exactly the 26 ASCII a-z — NOT RtlUpcaseUnicodeChar, which differs in 947. It VALIDATES FIRST then folds, so a fused scan+fold is wrong here: it leaves partially upcased text on the EINVAL path. That is the OPPOSITE of change 150, where strcpy_s does leave a partial copy. EINVAL also writes str[0]=0 even when the bound is 0) |
| [179](changes/179-strlwr-s/) | `_strlwr_s` (bounded byte lowercase) | `ucrtbase.dll!_strlwr_s` | **LANDED** — **5.92x** (up to 17.93x; byte sibling of 178 and the bounded form of 047. Fold pinned by a 255-value sweep to exactly ASCII A-Z, 0 differences; bytes >= 0x80 never fold, which the SIGNED vpcmpgtb gives for free. Validate-then-fold confirmed by its own probe rather than inherited — 178 behaves this way but 150's strcpy_s does the opposite, so the family cannot be reasoned about, only measured) |
| [180](changes/180-wcslwr-s/) | `_wcslwr_s` (bounded wide lowercase) | `ucrtbase.dll!_wcslwr_s` | **LANDED** — **4.10x** (up to 9.31x; bounded form of change 049, lowercase mirror of 178. Fold pinned by a 65535-unit sweep to exactly ASCII A-Z, 0 differences from the plain rule and 947 from RtlDowncaseUnicodeChar. Validate-then-fold probed independently rather than inherited) |
| [181](changes/181-strupr-s/) | `_strupr_s` (bounded byte uppercase) | `ucrtbase.dll!_strupr_s` | **LANDED** — **7.17x** (up to 23.99x, the largest of the `_s` case-fold family; bounded form of change 048. Fold pinned by a 255-value sweep to exactly ASCII a-z; bytes >= 0x80 never fold, which the SIGNED vpcmpgtb gives for free. ucrtbase's _strupr_s is itself slower than its _strlwr_s on identical input (227.68 vs 178.43 ns), so there was more to take) |
| [182](changes/182-strset-s/) | `_strset_s` (bounded byte fill) | `ucrtbase.dll!_strset_s` | **LANDED** — **3.60x** (up to 12.87x, ~32 GB/s; bounded form of change 077. A THIRD distinct `_s` error behaviour in one CRT: the fill family performs a PARTIAL FILL of numberOfElements-1 cells and only THEN empties the string, where 178-181 validate first and write nothing and change 150's strcpy_s leaves a partial copy. The first candidate assumed the 178-181 shape and was refuted on 407604/1M. Bound 0 writes NOTHING, so the correctness test also compares the invalid-parameter HANDLER HIT COUNT — the only observable effect there. Both outcomes fill, so one count feeds one vpbroadcastb loop) |
| [183](changes/183-wcsset-s/) | `_wcsset_s` (bounded wide fill) | `ucrtbase.dll!_wcsset_s` | **LANDED** — **1.99x** (up to 3.54x, ~33 GB/s; wide mirror of 182, fuzzed in its own right in the same probe run — 1M cases, 0 mismatches — because byte/wide pairs in this CRT are not automatically identical. Lower ratio than 182 for a measured reason: ucrtbase's wide fill already covers twice the memory per store, so the scalar baseline is ~2x faster per byte; our two implementations are within 4 % of each other in GB/s) |
| [184](changes/184-strnset-s/) | `_strnset_s` (bounded byte fill with count) | `ucrtbase.dll!_strnset_s` | **LANDED** — **2.99x** (up to 9.06x; bounded form of change 078, with TWO independent limits. Rule confirmed on the FIRST candidate, 1M cases: fill min(count, numberOfElements-1) then empty on the error path, min(count, length) on success, and `_TRUNCATE` is NOT special-cased — it just saturates. THE SCAN CANNOT BE CLIPPED BY count, because even at count 0 the return value still depends on whether a terminator exists inside the bound; only the fill is clipped, by one cmova. Correctness sweeps length x EVERY bound x EVERY count, ~1.8M triples) |
| [185](changes/185-wcsnset-s/) | `_wcsnset_s` (bounded wide fill with count) | `ucrtbase.dll!_wcsnset_s` | **LANDED** — **2.52x** (up to 5.18x; wide mirror of 184. Ends up with a HIGHER geomean than 183 on identical data because ucrtbase's own `_wcsnset_s` costs 87.86 ns where its `_wcsset_s` costs 63.09 ns for the same work — the second limit costs the scalar version ~40 %, and costs ours one cmova) |
| [186](changes/186-wtoi/) | `_wtoi` (= `_wtol`; wide decimal parse) | `ucrtbase.dll!_wtoi` | **LANDED** — **2.78x** (up to 5.95x). **This function was explicitly SCOPED OUT of this repo and the decision is now overturned.** changes/109-atoi64/RESULTS.md said a bit-exact reimpl "would need the CRT's full Unicode digit table, not an ASCII loop". Two sweeps replaced that assumption with a measurement: over all 65536 code units the digit set is EXACTLY 18 CONTIGUOUS BLOCKS OF TEN (180 members, 18 ascending runs, no exceptions) — the Unicode 3.0 Nd list, the SAME set change 166 found frozen in ntdll, re-measured here in a different DLL — and it is byte-identical under LC_ALL = C/en-US/ar-SA/ja-JP/th-TH/hi-IN/de-DE.UTF-8/.65001, so a fixed table is honest rather than C-locale-only. Whitespace is 26 units (the byte form skips six) and U+200B is NOT one. `_wtoi` and `_wtol` share one code address. Classifier split by frequency: ASCII in one lea/cmp, `c < 0x660` rejects everything else including NUL, fullwidth in three instructions, the other 16 blocks in ONE AVX2 pass (vpminuw+vpcmpeqw, xmm-only so no vzeroupper anywhere) |
| [187](changes/187-wtoi64/) | `_wtoi64` (wide 64-bit parse) | `ucrtbase.dll!_wtoi64` | **LANDED** — **2.23x** (up to 3.05x; the 64-bit result behaviour was RE-MEASURED rather than inherited from change 109 — byte and wide forms in this CRT have diverged before. Saturates at _I64_MAX/_I64_MIN, and a magnitude of exactly 2^63 IS accepted on the negative side, so -9223372036854775808 is exact. The 2^63 cap needs no register at all: "acc >= 2^63" is exactly "the top bit is set", which the sign flag answers for free) |
| [188](changes/188-wcstol/) | `wcstol` (wide base-0/2..36 parse) | `ucrtbase.dll!wcstol` | **LANDED** — **1.89x** (change 110's structure crossed with change 186's sets, and the CROSSING is where the traps are: non-ASCII digits work in EVERY base (U+0667=7 parses in base 8, U+0668=8 does not); the base>10 letters are ASCII-ONLY even though the digits are not (fullwidth 'f' is not a hex digit but fullwidth '9' is a decimal one); **the "0x" prefix zero may be ANY block's zero** — `<U+0660>x1f` really is 31 — which refuted the natural L'0' port on 1579/1.5M; but the 'x' itself stays ASCII-only. An invalid base raises the handler once, sets EINVAL and writes *endptr=nptr. SHAPE WAS MEASURED: a classifier-per-character CALL was bit-exact but regressed two classes (0.95x/0.85x); inlining ASCII digit, then letter, then fullwidth, then the blocks took it 1.16x -> 1.89x, and the Arabic-Indic class was ADDED to the bench first so the trade-off would show) |
| [189](changes/189-wcstoul/) | `wcstoul` (wide unsigned parse) | `ucrtbase.dll!wcstoul` | **LANDED** — **1.90x** (up to 2.39x; 188's body with change 111's unsigned tail: '-' negates modulo 2^32 with no error, and the limit is 2^32-1 REGARDLESS of sign where 188's moves with it. Both were fuzzed in the SAME run, 1.5M each, and the same run refuted the ASCII-only-prefix-zero variant for BOTH — the quirk is not specific to the signed form. Assembly note: `cmp r12, 0FFFFFFFFh` sign-extends to all-ones in 64-bit and never fires; the limit has to come from `mov r11d`) |
| [190](changes/190-wcstoi64/) | `_wcstoi64` (= `wcstoll`) | `ucrtbase.dll!_wcstoi64` | **LANDED** — **1.76x** (up to 2.18x; the overflow limit is SIGN-DEPENDENT — 2^63-1 positive, 2^63 negative — so "-9223372036854775808" is exact (errno 0) while "9223372036854775808" is ERANGE, and the same asymmetry holds in hex. NO 64-BIT DIVISION: the textbook cutoff/cutlim test needs a `div` per call, so `mul` supplies the high half instead and two `jc`s plus one limit compare do the whole guard. Needs one more long-lived value than 188, hence rbp for the accumulator, EIGHT pushes, and 28h rather than 20h of ABI-call padding) |
| [191](changes/191-wcstoui64/) | `_wcstoui64` (= `wcstoull`) | `ucrtbase.dll!_wcstoui64` | **LANDED** — **1.79x** (up to 2.21x; closes the wide parser family — six implementations now cover EIGHT exported names. Change 113's unsigned tail: the limit is 2^64-1 and does NOT move with the sign (the exact opposite of 190), '-' negates modulo 2^64 so "-1" is _UI64_MAX with errno 0 and "-18446744073709551615" is 1, but on OVERFLOW it returns _UI64_MAX regardless of sign rather than a negated magnitude. Because the limit is the full range the post-add limit compare can never fire — kept anyway so 190 and 191 stay one diff apart) |
| [192](changes/192-rtlarebitsclear/) | `RtlAreBitsClear` (bitmap: is a range all clear) | `ntdll.dll!RtlAreBitsClear` | **LANDED** — **3.45x** (up to 8.05x, 80 GB/s at a megabit; the complement of change 030, which ntdll still walks a byte at a time). The complement was NOT assumed — changes 123/124 both found the clear-side routines carry their own conventions — so every edge was re-derived against the live export: len 0 is FALSE (not "vacuously clear"), start+len is checked WITH THE CARRY so a ULONG-wrapping range is rejected rather than aliasing back in, and start==SizeOfBitMap is FALSE for any len. One `vptest` per 256 bits replaces change 030's compare+movmsk. SHAPE WAS FORCED BY MEASUREMENT: with the chunk test inside the loop a 32-bit range still paid the shared `vzeroupper` and measured 0.94x — a regression; hoisting it so a short range never touches ymm took 32 bits to 1.17x and 128 bits from 1.00x to 1.17x (re-measured 3x, since that class had been on the wrong side of the gate) |
| [193](changes/193-rtlistextunicode/) | `RtlIsTextUnicode` (encoding heuristic) | `ntdll.dll!RtlIsTextUnicode` | **LANDED** — **5.71x** (up to 8.70x). **The slowest routine found anywhere in this project's headroom survey**: 719 ns for 508 bytes, ~2.9 ns per 16-bit unit. Its cost SATURATES at ~735 ns from 1 KB to 128 KB, because `mov r14d,100h ; cmova` clamps the scan to 256 units — which is precisely why converting it wins at EVERY size at or above 512 bytes, and why that was measured before any work started. **The only change here whose contract could not be derived black-box**: three probe rounds failed to explain ASCII16 (set for 200 units of 'a' but not for 200 of a..z rotating) or STATISTICS (not the zero-byte-parity rule), so the shipped code was read (dumpbin /disasm, RVA 0x000D3A10). Both are TOTAL VARIATION sums of consecutive high/low bytes: ASCII16 iff lo_var<0x7F and hi_var==0, STATISTICS iff 3*hi_var<lo_var — which explains the 64-identical-unit boundary low>3*high exactly, since the only nonzero delta is the first, against 0. Two slots were misread first and the fuzz found both: the post-loop test is on the last HIGH byte (84090 mismatches of 3M, all in NULL_BYTES) and the CR/LF threshold divides by 40 not 10 (magic 0xCCCCCCCD with a TOTAL shift of 37; 8578 mismatches, delta-debugged to 19 x 'a' then one 0x1A). Vectorises because both statistics are |b[j]-b[j-2]| over one byte stream split by parity, so a single load at cursor-2 supplies every predecessor and two masked vpsadbw split and sum them — NO cross-iteration dependency. Bit-exact on the BOOL AND the rewritten *lpi over 21 ask-masks, 1.1M exhaustive short buffers and 400k fuzz. Scope: the DBCS lead-byte pass is out of reach on a DBCS ANSI code page, and the harness CHECKS that bit 0x400 never appears here rather than assuming it |
| [194](changes/194-i64toa-s/) | `_i64toa_s` (bounded 64-bit integer format) | `ucrtbase.dll!_i64toa_s` | **LANDED** — **2.32x** (up to 5.77x; the bounded form of change 057, whose shipped inner loop issues a 64-bit `div` PER DIGIT). The ERANGE path had to be READ, not fitted: no probed rule explained both "1234 size 4 -> 0 '3' '2' '1'" and "-1234 size 2 -> only buf[0] touched". The disassembly (RVA 0x00079D60, worker 0x00076F10) shows why — Buffer[0]=0 is written BEFORE the rest of the validation (so an invalid radix empties the buffer while size 0 leaves it untouched), and `size <= negative+1` fails BEFORE a single digit is emitted; digits go in least-significant-first and are reversed in place at the end, so a buffer that runs out keeps exactly the reversed prefix it held. Ours generates digits into a scratch FIRST so the fit is decided before touching the caller's buffer, then copies forward (success) or BACKWARD (the partial). TWO MEASURED FIXES turned a PARKED first cut into this: every power-of-two radix shifts instead of dividing (radix 2 had been issuing 64 divisions for a 1.00x tie -> 5.77x), and the byte-at-a-time copy that put base 36 at 0.92x became an 8-byte copy that is provably in bounds. Verified on the return, the WHOLE buffer, errno AND the handler count over 16 values x 35 radixes x every size 0..48, plus all 64 powers of two x 35 radixes x every size 1..70 |
| [195](changes/195-ui64toa-s/) | `_ui64toa_s` (bounded unsigned 64-bit format) | `ucrtbase.dll!_ui64toa_s` | **LANDED** — **2.38x** (up to 5.68x; change 194 with `negative` permanently 0, which is not an assumption: the signed entry at RVA 0x00079D60 computes (Radix==10 && Value<0) and calls 0x00079DAC, and the unsigned entry at 0x00079D90 passes a hard zero into the SAME address — the two are four instructions apart in the disassembly. With negative fixed at 0 the early ERANGE test reduces to SizeInChars <= 1) |
| [196](changes/196-i64tow-s/) | `_i64tow_s` (bounded wide 64-bit format) | `ucrtbase.dll!_i64tow_s` | **LANDED** — **2.29x** (up to 5.49x; the wide form. Byte and wide are separate functions at separate addresses, so identical behaviour was MEASURED, not inherited: 200000 random (value,size,radix) triples compared CHARACTER FOR CHARACTER including the untouched cells past the terminator, plus return and errno — 0 differences. SizeInChars counts CHARACTERS; the 2-digit decimal table holds a DWORD per entry so radix 10 still writes two digits per store and divides once per two digits; the copy still moves 8 bytes — four characters — at a time. Within 2 % of the byte form at every class, because the work is dominated by the division chain rather than the stores) |
| [197](changes/197-ui64tow-s/) | `_ui64tow_s` (bounded unsigned wide 64-bit format) | `ucrtbase.dll!_ui64tow_s` | **LANDED** — **2.06x** (up to 3.63x; closes the bounded 64-bit formatter family — 194 byte signed, 195 byte unsigned, 196 wide signed, 197 wide unsigned, all four live-proven together. The narrowest of the four for two visible reasons: `42` base 10 is 1.07x because ucrtbase's own unsigned wide entry is already its fastest small case at 3.61 ns, and 64 bits base 2 is 46.66 ns against 31.34 for the signed wide form — 64 wide digits is 128 bytes of scratch to copy, the one place the wide forms genuinely pay for the extra byte per character) |
| [198](changes/198-itoa-s/) | `_itoa_s` / `_ltoa_s` (bounded 32-bit integer format) | `ucrtbase.dll!_itoa_s` | **LANDED** — **1.73x** (up to 2.82x; the bounded form of change 056 and the 32-bit sibling of 194. ONE implementation, TWO exports: `_ltoa_s` is a separate export the compiler laid out with the branch inverted, and the harness drives BOTH rather than assuming it. The 32-bit width is the whole contract difference — at any radix but 10 the value formats as UNSIGNED 32-bit, so `_itoa_s(-1,b,n,16)` is "ffffffff", eight f's, not 194's sixteen. THREE THINGS WERE MEASURED, NOT ASSUMED: (1) the buffer-fit check is HOISTED via a `maxdig[radix]` table — per-digit bound checks cost ~1.5 cycles each and took base 36 to 0.88x and base 2 from 3.98x to 2.51x; (2) a reciprocal multiply is NOT faster than Zen 4's divider here — the 2^38 magic needing `shrd` measured 0.77x and even the 2^64 magic, whose quotient lands in rdx with no shift at all, only reached 0.95x, so the `div` stayed; (3) the ERANGE path used to generate digits into a scratch and then walk them back out to reproduce ucrtbase's reversed leftover — but the emit loop already runs least-significant-first, which IS that order, so it now writes straight into the caller's buffer and the second pass is gone (0.93x -> 1.07x). ERANGE is a structural tie at best: ~21 ns against ~6 ns for the success classes, because the contract obliges us to CALL ucrtbase's exported `_errno` and `_invalid_parameter_noinfo` where the shipped code reaches the same state internally) |
| [199](changes/199-ultoa-s/) | `_ultoa_s` (bounded unsigned 32-bit format) | `ucrtbase.dll!_ultoa_s` | **LANDED** — **1.77x** (up to 2.84x; change 198 with `negative` permanently 0, kept one diff apart the way 190/191 were. Its base-36 class is what exposed the last real inefficiency in the family: a 32-bit value is at most 7 digits in base 36, so the final copy ALWAYS fell into a counted byte loop, and that loop — not the divides — was the entire deficit against ucrtbase, which reverses in place and never copies. Width-dispatching it (two overlapping 4-byte moves for 4..7 digits, and a branch-free first/last/middle for 1..3) took the class from 10.06 ns to 8.33 ns, 0.93x -> 1.14x) |
| [200](changes/200-itow-s/) | `_itow_s` / `_ltow_s` (bounded wide 32-bit format) | `ucrtbase.dll!_itow_s` | **LANDED** — **1.65x** (up to **3.57x**; the wide form, again ONE implementation for TWO exports with `_ltow_s` patched and driven separately rather than assumed. Base 2 is the widest win in the family: a 32-bit value is 32 digits and every power-of-two radix shifts instead of dividing, against ucrtbase's divide-per-digit. The wide ERANGE class is an honest TIE — repeated runs of the same binary read between 0.94x and 1.05x — and that is recorded in RESULTS.md rather than papered over) |
| [201](changes/201-ultow-s/) | `_ultow_s` (bounded unsigned wide 32-bit format) | `ucrtbase.dll!_ultow_s` | **LANDED** — **1.55x** (up to 2.72x; closes the 32-bit formatter family — FOUR implementations now cover SIX exported names. The tightest of the four: with no sign test and an unsigned magnitude ucrtbase's own two-digit case is already near its floor at 3.62 ns, so the smallest class is a genuine tie rather than a win, and the branch-free 1..3-cell copy is what brought it back over the line) |
| [202](changes/202-convertguidtostringw/) | `ConvertGuidToStringW` (GUID -> `{XXXXXXXX-...}`) | `iphlpapi.dll!ConvertGuidToStringW` | **LANDED** — **37.30x** (up to **110.89x** — the largest win in the project, and the first target from `iphlpapi.dll`. The shipped routine DOES NOT FORMAT THE GUID: it spills the eleven fields as varargs, loads the literal "{%08lX-%04X-%04X-%02X%02X-%02X%02X%02X%02X%02X%02X}" and calls a StringCchPrintfW clone that re-parses that format on EVERY call and dispatches each conversion through a per-character helper — ~305 ns to write 38 characters. The trick is that Data1/2/3 are little-endian integers printed most-significant-nibble first while Data4 prints in memory order, so the sixteen bytes appear in print order 3,2,1,0, 5,4, 7,6, 8..15 — a SINGLE vpshufb — after which the whole conversion is branch-free (nibble split, vpunpck interleave, vpshufb through a hex table, vpmovzxbw to UTF-16) over a pre-stored 39-cell template. AVX2 only. THREE distinct failure lengths had to be reproduced exactly: cch 0 leaves the buffer UNTOUCHED, 1..38 writes a truncated prefix plus a terminator, and cch >= 0x80000000 returns 122 with String[0]=0 rather than 87. THIS IS THE CHANGE THAT FOUND THE ABI BUG — its first cut used xmm6/xmm7 and reported 0.00 ns while being bit-exact, which exposed 16 implementations violating the Win64 register contract; see tools/abi-check) |
| [203](changes/203-convertguidtostringa/) | `ConvertGuidToStringA` (narrow GUID -> `{XXXXXXXX-...}`) | `iphlpapi.dll!ConvertGuidToStringA` | **LANDED** — **35.58x** (up to **97.16x**; the narrow sibling of 202, and the same cause — 263 ns to write 38 characters through a StringCchPrintfA clone that re-parses its format on every call. THE CONTRACT WAS MEASURED, NOT INHERITED: it is a separate export at a different address, and this project has found A/W pairs carrying different conventions before, so probes/cgsa.c drove BOTH over 200k random (GUID, cch) pairs and compared character for character — 0 return-value differences, 0 buffer differences — and settled one ordering detail, that the NULL test precedes the length test so (NULL, NULL, 0) is 87 and not 122. CHEAPER than the wide form: 202 spends a vpmovzxbw pair and two vextracti128 widening 32 ASCII bytes into UTF-16 cells, and here the vpshufb output IS the answer, so the tail is five stores whose last two overlap deliberately on out[29..32] rather than masking a 12-byte run. That also makes the truncating class 14.03x against 202's 10.38x. Both iphlpapi targets are now live-proven in one harness, patched and driven separately) |
